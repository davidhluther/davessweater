One file per proposed experiment: `<date>-<slug>.md` with a front-matter block (`name`, `hypothesis`,
`metric`, `instrument`, `baseline_window`, `measurement_window_days`, `change`, `revert_cmd`, `cost_usd: 0`,
`ahrefs_units: 0`, `approved: false`) and the reasoning below it. `approved` is David's field: he flips it
here or completes the matching Todoist task; the lane applies only after one of those. Public repo.
