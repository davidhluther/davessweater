# DAVESSWEATER EXPERIMENTS — week of 2026-09-25    run: PARTIAL

Run `2026-09-25-6caf8a` · started 2026-09-25T10:41:38-04:00 · mode live · approval gate ON (a completed Todoist decision is required before any change is applied)

## THIS WEEK  (proposed / applied / measuring / concluded)

- Nothing changed state this week.
- No active experiment.

## RESULTS  (metric · baseline → now · verdict: kept / reverted / inconclusive)

- No measurement window closed this week.

## NEXT PROPOSAL

- None proposed this week (see RUN HEALTH if that is a failure).

## DECISIONS FOR DAVID [≤3, each with a default]

- [ ] **Give the ds-experiments worktree a node_modules so the lane can actually apply an experiment?** — Root cause found, and it is NOT last run's dirty-worktree theory (that one is cleared — af941fd6 committed both stray files and this run rebased clean, preflight passed, and all four apply conditions were satisfied for the first time since approval). The lane still could not apply, for a deeper reason: ~/Projects/worktrees/ds-experiments has NO node_modules. RUNBOOK step 4 mandates three pre-apply checks; check_function_budget.py passes (pure python, 10/10 bundles), but 'npx vitest run' dies with MODULE_NOT_FOUND (vitest.config.ts cannot resolve vite) and 'npm run lint' cannot run either. 'npm install' is DENIED by the committed allowlist, so the lane cannot fix this itself, and it never will: this repeats every week and the lane can never apply ANY experiment under the current setup. foscoe-title-meta-ctr has now been approved and unapplied for 7 days across three runs. The edit itself was made and verified this run (title 58 chars, description 140 chars, foscoe-only ternary, 16 towns untouched, src/ only) and then reversed so it would not dirty next week's run. Fix options: (a) one-time 'npm ci' in the worktree — /node_modules is gitignored so it cannot dirty the tree, but it needs a refresh whenever package.json changes; (b) symlink the main checkout's node_modules into the worktree — cheapest, one command, though the main checkout currently sits on branch leaf-v1-calibration-2026-09-04 so its dependency tree is not guaranteed to match main's package.json; (c) allowlist 'Bash(npm ci)' for this lane so it self-heals — note the deny list bars npm install deliberately (traced-payload risk, the 08-21 outage); (d) relax RUNBOOK step 4 to require only the checks the worktree can run — honest, but it ships metadata changes to a PUBLIC production site with no test or lint gate. Evidence on risk if you pick (d): all 36 test files live under src/lib/__tests__ plus sitemap/v1Route, and none covers page metadata, so this specific change is very unlikely to be test-covered — but 'unlikely to be covered' is not 'the check passed'. Cash $0, units 0 either way.
      default: option (a) — David runs 'npm ci' once in ~/Projects/worktrees/ds-experiments; the lane then applies foscoe-title-meta-ctr on 2026-09-22 with no further approval, since d-20260908-5ef7 already stands · Todoist task created by the run

## RUN HEALTH

steps 5 ok · 1 failed · 3 skipped · 4.2 min wall · Ahrefs units 0 (must be 0) · cash $0 (must be $0) · steps failed: apply: npx vitest run: Cannot find module 'vitest/config' — node_mo
  - **FAILED** `apply`: npx vitest run: Cannot find module 'vitest/config' — node_modules absent in this worktree, npm install/ci denied by allowlist; npm run lint blocked same reason. check_function_budget.py passed (10/10). foscoe-title-meta-ctr remains APPROVED (d-20260908-5ef7) but cannot apply until node_modules exists (decision d-20260916-2e88, open since 2026-09-16, never filed to Todoist)
  - skipped `read-decisions`: only open decision is d-20260916-2e88 (node_modules fix), todoist_id null — never filed; nothing to read back. Will file in step 9.
  - skipped `measure`: active_experiment is null — foscoe-title-meta-ctr is approved but never applied, so nothing is in a measuring state
  - skipped `propose`: foscoe-title-meta-ctr is still an unconcluded experiment (approved d-20260908-5ef7, never applied) — one experiment at a time means no new proposal until it applies or is abandoned; proceeding to attempt apply in step 4 instead
- cost line: cash $0 · units 0 · sonnet-5 · ~25 tool calls
- State: last_run 2026-09-16T12:18:30-04:00 → this run; completed experiments 0; push enabled; lock present at digest time (the run removes it at finish)
- Next fire: Wednesday 08:00 (routine ds-experiments-weekly); dead-man check Thursday 08:00.

_Delivered by the ds-experiments lane wrapper (Refactor 2 skeleton). $0 cash, 0 Ahrefs units, one reversible experiment at a time, applied only after a completed decision. Silence is never a state: if this digest did not arrive on a Wednesday, the Thursday dead-man email says so._
