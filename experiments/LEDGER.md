# DavesSweater experiments — LEDGER (Refactor 2 Lane 4)

The durable record of every experiment this lane proposes, applies, measures, and concludes. One row per
experiment, updated in place as it moves; the weekly digest is a view of this file plus the run record.
Rules: one experiment at a time · $0 cash · 0 Ahrefs units · every change carries a one-command revert
recorded BEFORE it is applied · applying needs a completed Todoist decision until David lifts the gate.
This repository is public; write every row accordingly.

| name | proposed | hypothesis | metric (instrument) | baseline | applied (sha) | window ends | revert | verdict |
|---|---|---|---|---|---|---|---|---|
| foscoe-title-meta-ctr | 2026-09-08 | A snippet that names the deliverable and fits in ~155 chars lifts CTR on foscoe-intent queries off 0.00% | CTR on foscoe-named queries (Search Console) | 535 impressions / 0 clicks / 0.00% / pos 10.3 (2026-08-10..09-06); re-verified 2026-09-09: 534 / 0 / 0.00% / pos 10.4; re-verified 2026-09-16 (2026-08-18..09-14): 403 / 0 / 0.00% / pos ~10.4 | — (proposed; dry run applied nothing) | — | git revert of the applying commit | APPROVED, still not applied (third attempted run, 2026-09-25) — `d-20260908-5ef7` completed 2026-09-09 07:43 EDT with no refusing comment, so the GATE IS SATISFIED. Two prior runs failed to apply (2026-09-09: dry_run/push_enabled off; 2026-09-16: dirty worktree). This run (`2026-09-25-6caf8a`) rebased clean and reached step 4, but pre-apply checks failed: `~/Projects/worktrees/ds-experiments` has no `node_modules`, so `npx vitest run` dies with `Cannot find module 'vitest/config'` and `npm run lint` cannot run; `npm install`/`npm ci` are denied by the committed allowlist. `check_function_budget.py` passed (10/10 bundles) but that alone isn't sufficient gate to ship a public-site change on. This is the same root cause first found 2026-09-16 (decision `d-20260916-2e88`) — it was raised but never filed to Todoist (`todoist_id: null`) for 9 days; filed this run. No re-baseline pulled this run (steps 2/3 skipped — no active experiment, and proposing a second experiment while foscoe sits unconcluded would break the one-at-a-time rule); the 2026-09-16 numbers (403 / 0 / 0.00% / pos ~10.4) stand as the most recent baseline. Next apply attempt needs `npm ci` run once in the worktree (David, per `d-20260916-2e88`). |
