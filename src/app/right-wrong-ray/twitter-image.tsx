// Same card for the twitter:image slot (some crawlers ignore the og:image
// fallback when the card type is summary_large_image).
export { default, alt, size, contentType } from "./opengraph-image";
